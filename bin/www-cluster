#!/usr/bin/env node
const sticky = require('sticky-cluster');
const {normalizePort} = require('../utils');
const port = normalizePort(process.env.PORT || '3000');
sticky((cb) => cb(require('./www')), {
  port,
  debug: true
});
